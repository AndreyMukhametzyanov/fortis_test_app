<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>New recipe</h1>
  <%= link_to "← Back to recipes", chef_recipes_path(@recipe.chef) %>
</div>

<%= render "form", recipe: @recipe %>
